<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aldy.uas10116158">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".teman.DetailActivity"></activity>
        <activity android:name=".teman.AddTemanActivity" />
        <activity
            android:name=".registerlogin.SignUpActivity"
            android:parentActivityName=".registerlogin.LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".registerlogin.LoginActivity" />
        </activity>
        <activity
            android:name=".registerlogin.LoginActivity"
            android:launchMode="singleTop" />
        <activity android:name=".viewpager.IntroActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>